# Terraform
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.backup
terraform/*.tfvars
# .terraform.lock.hcl is deliberately NOT ignored — it is the only thing that
# pins which provider build reaches production. See terraform/README.md.

# Claude Code local settings
.claude/settings.local.json
.claude/agents/

# Node
node_modules/
dist/

# Playwright
test-results/
playwright-report/
.playwright/
debug.log
.tmp/

# Local assistant configuration and review documents. These stay on disk and
# keep working locally; they are simply not part of what the project publishes.
.claude/
CLAUDE.md
Codex_Review.md

# Generated review screenshots
screenshots/

# OS
.DS_Store
Thumbs.db

# Debug output from `npm run pixi:visual-check` when a scene breaches its
# budget. The references beside them are committed; these are not.
tests/fixtures/reference-frames/**/*.actual.png
